import Logo from "./Logo";
import NavItem from "./NavIten";
const Heading = () => {
    return (
      <div className="heading">
        <Logo />
        <NavItem />
      </div>
    );
  };

export default Heading;